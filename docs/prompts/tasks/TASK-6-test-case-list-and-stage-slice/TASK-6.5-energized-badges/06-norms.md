# Norms Prompt

Apply the project norms to this task:

Task: `6.5 Add energized dynamic test badge derivation for EV inspection steps and expose equivalent accessibility information.`

Check the proposed work against these norms:

- Safety metadata must be visible and accessible
- VoiceOver must announce energized safety state with meaningful text
- Status and safety states must not rely on color alone
- Derived policy should live near content/model normalization, not inside ad hoc view string checks
- TDD for model and view helper behavior
- Feature scenarios and JSON-content requirements are the acceptance backbone
- Full structured `Strings` and accessibility identifier cleanup belongs to TASK#6.6
- Branch name is `codex/task-6.5-energized-badges`
- PR titles must start with `TASK#{TASK NUMBER}:`, using `TASK#6.5: Add energized badges`
- Commit titles must be 52 characters or fewer
- Do not build automatically; capture build and validation as user-run unless explicitly asked

Return any adjustments needed before implementation.

Expected PR title:

`TASK#6.5: Add energized badges`

Recommended commit title candidates:

- `Add TASK#6.5 prompt run`
- `Add energized badge policy`
- `Expose energized accessibility`
