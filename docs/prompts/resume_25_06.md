implement from 7.6 to 7.9 sequentially.
For each task you'll:
- create the SPDD prompt; checkout to a new branch; commit it; execute prompt in new session;
- commit changes; file PR; checkout to next task; PR will point to the previous task (if has dependency) or develop (if there's no dependency from previous task).
- repeat.

If there're tasks which can be parallelized, propose a multi-agent approach and request manual review before proceeding.
Otherwise keep it sequential.