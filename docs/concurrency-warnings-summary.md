# Concurrency Warning Summary

Build log reviewed: `/Users/dmh/Downloads/Build FSAEInspectionChecklist_2026-07-04T23-46-57.txt`

## Problem Statement

The project is using default MainActor isolation while many domain, persistence, validation, and decoding declarations are intended to be executor-independent. The warning cleanup must restore the intended concurrency architecture, not push services and model code onto MainActor.

## Fixed This Session

| Problem statement | Location | Proposed fix | Possible impacts |
| --- | --- | --- | --- |
| `PersistedValidationIssue.init(issue:)` was `@MainActor`, so a persistence actor could not map validation issues without losing the global actor. This was fixed in the active session by removing the initializer's MainActor isolation and targeted persistence tests passed. | `FSAECertification/FSAEInspectionChecklist/FSAEInspectionChecklist/Features/Persistence/Services/TestCaseJSONPersistenceService.swift:27`, `:202`, `:324` | Keep the DTO initializer nonisolated. Also make its pure helper dependencies nonisolated, including `ValidationIssue.Code.stepID` and `storageCode`. | Exposes follow-on warnings where helper properties were accidentally MainActor-isolated. Correctly keeps JSON persistence off the UI executor. |
| `InMemoryDraftStore` looked suspicious because it is `@MainActor`, but its isolation is for UI-owned mutable draft state. The issue is not the store; the issue is value helpers leaking MainActor into services. | `FSAECertification/FSAEInspectionChecklist/FSAEInspectionChecklist/Features/Persistence/Models/InspectionDraftModels.swift:34` | Keep `InMemoryDraftStore` MainActor unless it becomes a real actor. Make `StepResult` a nonisolated value type so validation can use snapshots off MainActor. | UI draft updates remain synchronous and simple. Persistence and validation should receive value snapshots, not the mutable store. |

## Existing Problems From Attachment

| Problem statement | Location | Proposed fix | Possible impacts |
| --- | --- | --- | --- |
| App coordinator default arguments read `MockInspectionData.teams` and `.stages`, which default to MainActor isolation. This is a default-argument and mock-data boundary problem, not a reason to move app setup off MainActor incorrectly. | `FSAECertification/FSAEInspectionChecklist/FSAEInspectionChecklist/Features/SessionFlow/Coordinators/AppCoordinator.swift:16`, `:17`; definitions at `Features/InspectionContent/Models/InspectionModels.swift:250`, `:256` | Prefer making `AppCoordinator.init` explicitly MainActor-friendly without nonisolated default argument evaluation, or move mock fixtures behind a MainActor app factory. Only mark mock constants nonisolated if their values are Sendable and UI-free. | Changing defaults can affect previews and tests that rely on implicit mock data. Making mock data nonisolated while it contains UI types may hide a real boundary issue. |
| Persistence DTO conversion is blocked by an accidental MainActor initializer and pure storage helpers. This repeats at draft save and submitted snapshot save. | `Features/Persistence/Services/TestCaseJSONPersistenceService.swift:27`, `:202`, `:324`, `:550`, `:562` | Keep `PersistedValidationIssue` and `ValidationIssue.Code` storage helpers nonisolated pure value code. Do not mark `TestCaseJSONPersistenceService` MainActor. | JSON output should remain identical. Add or keep tests for validation summaries in draft and submitted snapshot files. |
| `SessionKey` is an actor-internal dictionary key, but its `Hashable` conformance and initializers are treated as MainActor. This infects `InspectionEventStore` actor methods. | `Features/SessionFlow/Store/InspectionEventStore.swift:170`, `:172`, `:210`, `:227`, `:258`, `:279`, `:347`, `:387`, `:397`, `:401` | Make `SessionKey` and its initializers explicitly nonisolated value code. Keep `InspectionEventStore` as an actor and keep session mutation inside it. | Should not alter behavior. Verify session lookup, save draft, access-control, and team/session isolation tests because dictionary-key equality is core state. |
| `InspectionTestCase` has custom `Codable` methods that default to MainActor, so its Codable conformance cannot be used by persistence or content services safely. | `Features/TestCaseExecution/Models/InspectionTestCaseModels.swift:12`, `:103`, `:134` | Make the custom `Codable` implementation nonisolated, including private decoding helpers that construct steps. | High impact if decoding shape changes. Verify legacy item IDs, rule reference fallback, badge decoding, ordered steps, and persistence round trips. |
| Test case safety badge derivation calls helpers that default to MainActor despite being pure value logic. | `Features/TestCaseExecution/Models/InspectionTestCaseModels.swift:64`, `:69`, `:74`, `:84`; `Features/TestStepExecution/Models/InspectionTestStepModels.swift:180` | Make safety badge derivation and collection merging nonisolated pure code, or move derived display badges to a UI/view-state layer if it is presentation-only. | If moved, update views and tests that expect `displaySafetyBadges` on the domain model. If kept in domain, verify no SwiftUI types enter the path. |
| Validation rules are Sendable service code, but default rule arrays and helper functions default to MainActor. Sendable closures then cannot call evidence and measurement helpers safely. | `Features/Validation/ValidationService.swift:51`, `:80`, `:143`, `:156`, `:157`, `:158`, `:165`, `:166`, `:167`, `:174`, `:224`, `:233`, `:248`, `:261` | Keep `InspectionValidationService`, rule factories, evidence requirements, localization-key selection, message construction, and measurement argument builders nonisolated pure value code. | Validation messages and localization arguments are user-visible and persisted. Verify validation unit tests, measurement bounds, evidence requirements, and failure-note behavior. |
| `StepResult` is a snapshot value used by validation, but its initializer defaults to MainActor. This forces nonisolated test-case validation to cross into UI isolation. | `Features/Persistence/Models/InspectionDraftModels.swift:7`, `:19`; usage at `Features/TestCaseExecution/Models/InspectionTestCaseModels.swift:372` | Make `StepResult` explicitly nonisolated while leaving `InMemoryDraftStore` MainActor. Consider moving `StepResult` to a domain model file separate from the MainActor store. | Low behavioral risk, but file placement matters. Verify stage row state and validation summary tests. |
| `ValidationIssue.stepID` private helper defaults to MainActor and blocks nonisolated validation summaries. | `Features/TestCaseExecution/Models/InspectionTestCaseModels.swift:391`, `:443`, `:447` | Make this helper nonisolated or expose a nonisolated `stepID` on `ValidationIssue.Code` once and reuse it consistently. | Consolidating helper logic reduces duplication with persistence. Verify first blocking step and progress calculations. |
| `InspectionTestStep`, `InspectionTestStepType`, and `InspectionSafetyBadge` custom decoding defaults to MainActor. These are content and persistence values and must decode off MainActor. | `Features/TestStepExecution/Models/InspectionTestStepModels.swift:9`, `:76`, `:116`, `:149`, `:164`, `:200` | Make the custom `Decodable` implementations nonisolated. Keep the raw value enums Sendable and domain-safe. | High impact for bundled JSON compatibility. Verify unsupported step type errors, legacy badge names, and content loading tests. |
| Domain enums expose SwiftUI `Color` values while also being used as nonisolated Sendable model types. The compiler is warning because the color palette is MainActor-isolated. | `Features/TestStepExecution/Models/InspectionTestStepModels.swift:137`, `:239`; palette at `Features/InspectionContent/Models/InspectionModels.swift:323`-`:332` | Prefer moving `color` properties to UI/view-state extensions or adapters. If retained, keep them MainActor and ensure domain, decoding, validation, and persistence never call them. | Could require view updates but clarifies architecture. Prevents SwiftUI from leaking into persistence-safe domain values. |

## Verification Checklist

- Full app build emits zero Swift concurrency warnings.
- `TestCaseJSONPersistenceServiceTests` pass after DTO and helper isolation fixes.
- Validation service tests pass after rule and helper isolation fixes.
- Content decoding tests pass after Codable isolation fixes.
- Inspection event store tests pass after `SessionKey` fixes.
- View/coordinator tests or manual checks pass after moving color and display helpers into UI adapters.

## Implementation Notes - 2026-07-05

- Kept UI-owned coordinator state on `@MainActor`, but moved `AppCoordinator` mock fixture defaults into a MainActor convenience initializer so default argument evaluation no longer reaches mock data from a nonisolated context.
- Kept persistence, validation, decoding, and session storage off MainActor by marking pure value witnesses/helpers `nonisolated`: custom `Codable` methods, `StepResult`, `PersistedValidationIssue.init(issue:)`, validation rule factories/helpers, and actor-internal `SessionKey`.
- Moved `InspectionTestStepType.color` and `InspectionOutcome.color` out of Sendable domain models and into the SwiftUI design system. Domain models no longer import SwiftUI only for presentation colors.
- Saved verification logs at `docs/concurrency-warden-build.log` and `docs/concurrency-warden-test.log`.
- `xcodebuild build -scheme FSAEInspectionChecklist -project FSAECertification/FSAEInspectionChecklist/FSAEInspectionChecklist.xcodeproj -destination 'platform=macOS' -derivedDataPath ./.DerivedData CODE_SIGNING_ALLOWED=NO` succeeded with no Swift concurrency warnings. The remaining `warning:` line is Xcode AppIntents metadata extraction noise.
- `xcodebuild test -scheme FSAEInspectionChecklist -project FSAECertification/FSAEInspectionChecklist/FSAEInspectionChecklist.xcodeproj -destination 'platform=macOS' -derivedDataPath ./.DerivedData CODE_SIGNING_ALLOWED=NO` succeeded after running outside the sandbox so the macOS test runner could communicate with `testmanagerd`.
